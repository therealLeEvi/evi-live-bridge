{
  "name": "evi-live-bridge",
  "version": "3.6.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node --use-system-ca bridge/server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
